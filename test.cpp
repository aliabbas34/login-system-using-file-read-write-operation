#include <iostream>
#include <stdio.h>
#include <conio.h>
using namespace std;
int main() {
    // // Write C++ code here
    // cout << "Hello world!"<<endl;
    // char c;
    // string s;
    // int i=0;
    // while(c=getch()!='x'){
    // s[i]=c;
    // cout<<s[i]<<endl;
    // i++;
    // }
    // cout<<s[0]<<endl;
    // cout<<"end"<<endl;
    // return 0;
    int a='1'-'0';
    cout<<a<<endl;
    a=0-'0';
    cout<<a<<endl;

    // int check=1;
        // while(check){
        // cout<<"Your password may contain alphabets , digits and special characters(only @,!,-,_,.(comma not included))"<<endl;

        // cout<<"select a password : ";
        // check=0;
        // char c;
        // int i=0;
        // while(c=getch()!='\r'){
        //     if(((c-33)>=0)&&((c-33)<=93)){
        //         if((int(c)>=33 && int(c)<=47)&&(int(c)>=58 && int(c)<=64)&&(int(c)>=91 && int(c)<=96)&&(int(c)>=123 && int(c)<=126)&&(!(c=='@'||c=='!'||c=='-'||c=='_'||c=='.')))
        //         check=1;
        //         password[i]=c;
        //         cout<<"*";
        //         i++;
        //     }
            
            //if((c-int('a')>=0)&&(c-int('a')<=25)&&(c-int('0')>=0)&&(c-int('0')<=9)&&(c=='@'||c=='!'||c=='-'||c=='_'||c=='.'))
        //}
        // for(int i=0;i<password.size();i++){

        // }
        //}
        // cin>>password;

}